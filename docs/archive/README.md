# docs/archive/

Superseded planning work. **The current plan is [`../../ROADMAP.md`](../../ROADMAP.md).**

These are kept because they carry implementation detail (per-phase steps,
entity-model reasoning, Green Invoice API quirks, the workflow discovery Q&A)
that ROADMAP.md summarises rather than repeats. Read them for the *how*, never
for the *direction* — the direction here is out of date (e.g. these assume
Daf Kesher stays as the identity source; it does not — see ROADMAP §5 Phase 4).
